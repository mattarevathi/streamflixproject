# Name shown in logs and (later) in monitoring tools
spring.application.name=streamflix

# Port the embedded web server listens on.
# Kept here (not hard-coded in Java) so it's easy to change later, e.g. for AWS Elastic Beanstalk.
server.port=8080
